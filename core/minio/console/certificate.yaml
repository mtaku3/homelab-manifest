apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: minio-console

spec:
  secretName: minio-console-tls
  dnsNames:
    - miniocon.mtaku3.com
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
