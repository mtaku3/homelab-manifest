apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: authentik

spec:
  secretName: authentik-tls
  dnsNames:
    - authentik.mtaku3.com
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer

