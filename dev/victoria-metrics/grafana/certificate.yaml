apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: grafana-tls
  namespace: victoria-metrics
spec:
  secretName: grafana-tls-cert
  dnsNames:
    - grafana.mtaku3.com
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
