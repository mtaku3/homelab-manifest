apiVersion: v1
kind: Service
metadata:
  name: happy-server
  namespace: happy
  labels:
    app.kubernetes.io/name: happy-server
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: happy-server
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
