apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: web-tls
  namespace: paseo
spec:
  secretName: web-tls-cert
  dnsNames:
    - paseo.mtaku3.com
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
