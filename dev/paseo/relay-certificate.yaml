apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: relay-tls
  namespace: paseo
spec:
  secretName: relay-tls-cert
  dnsNames:
    - relay.paseo.mtaku3.com
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
