apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: web-tls
  namespace: mt4jm
spec:
  secretName: web-tls-cert
  dnsNames:
    - mt4jm.mtaku3.com
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
