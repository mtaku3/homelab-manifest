apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: web-tls
  namespace: obot
spec:
  secretName: web-tls-cert
  dnsNames:
    - obot.mtaku3.com
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
