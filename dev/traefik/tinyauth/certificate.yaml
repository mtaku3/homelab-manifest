apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tinyauth-tls
  namespace: traefik
spec:
  secretName: tinyauth-tls-cert
  dnsNames:
    - tinyauth.mtaku3.com
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer

