apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: registry-tls
  namespace: gitlab
spec:
  secretName: registry-tls-cert
  dnsNames:
    - registry.mtaku3.com
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
