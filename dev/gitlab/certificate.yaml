apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: web-tls
  namespace: gitlab
spec:
  secretName: web-tls-cert
  dnsNames:
    - gitlab.mtaku3.com
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
