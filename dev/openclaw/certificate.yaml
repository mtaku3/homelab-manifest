apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: openclaw-tls
  namespace: openclaw
spec:
  secretName: openclaw-tls-cert
  dnsNames:
    - openclaw.mtaku3.com
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
