apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: web-tls
  namespace: cloudcli
spec:
  secretName: web-tls-cert
  dnsNames:
    - cloudcli.mtaku3.com
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
